import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container:{
        flexDirection: "row",
        marginVertical: 5,
    },
    title: {
        color: "#DD5E56",
        fontWeight: "bold",
        fontSize: 14,
    },
    subtitle: {
        fontSize: 14,
        fontWeight: "bold",
    },
});